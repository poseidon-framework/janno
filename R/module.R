#' poseidon2 command line interface module functions
#'
#' @param input_file character. Path to input file
#' @param filter_file character. Path to filter file
#' @param output_directory character. Path to output directory
#' @param log_directory character. Path to directory for log files and temporary data
#' @param output_format character. Output format of conversion (e.g. eigenstrat)
#' @param input_package character. Path to input poseidon2 package
#' @param input_janno_file_or_packages character. Path to input janno file or poseidon2 package
#' @param uncalibrated_ages_BP character. One or multiple radiocarbon date ages BP (e.g. "3000" or "3000,3100,3050" or c("2000", "2000,2300,2100"))
#' @param standard_deviations character. One or multiple standard deviations (1 sigma ±) (e.g. "30" or "30,40,50" or c("20", "20,30,70"))
#' 
#' @name cli_modules
NULL